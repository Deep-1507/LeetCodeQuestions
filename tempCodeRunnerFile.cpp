for (int i = 0; i < n; i++)
        // {
        //      if (A[i] == 0)
        //      {
                
        //          cout <<"Minimum number of elements to be dropped " << 0 << endl;
        //      }
        //      else if(count % 2==0){
        //         cout<<"Minimum number of elements to be dropped " << 0 << endl;
        //      }
        //      else if(count % 2 !=0){
        //         cout<<"Minimum number of elements to be dropped " << count << endl;
        //      }
        // }